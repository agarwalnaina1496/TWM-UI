/**
 * Central export for all TypeScript types.
 * Import from '@/types' rather than individual files.
 *
 * @example
 * import type { Trip, TripMatcherResponse } from '@/types'
 */
export type * from './api.types'
export type * from './trip.types'
