# GA4 Measurement ID (TWM-149). Set this ONLY in the production Vercel
# environment — leaving it unset in dev/preview is what keeps those
# environments from sending traffic to production analytics (see
# src/lib/analytics.js and the TWM Analytics Specification linked from
# TWM-150). This is a public client-side identifier, not a secret.
VITE_GA_MEASUREMENT_ID=G-E8T6FJ5XEV
